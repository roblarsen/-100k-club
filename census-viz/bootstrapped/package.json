{
  "name": "bubble-chart-d3-ts",
  "version": "1.0.0",
  "description": "A bubble chart visualization using D3.js and TypeScript",
  "scripts": {
    "copy-img": "cp -vR ./img ./dist",
    "start": "npm run copy-img && parcel index.html",
    "build": "npm run copy-img && parcel build index.html"
  },
  "dependencies": {
    "d3": "^7.8.4"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.14.4",
    "@parcel/transformer-webmanifest": "^2.14.4",
    "parcel": "^2.9.3",
    "typescript": "^5.1.0"
  }
}
